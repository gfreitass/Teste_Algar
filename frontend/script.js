import { response } from "express";

function buscarProduto(){
  var regexp = /não gela/ig;
  response;
  {
    var OK = regexp.exec();
    if (!OK)
    window.alert(RegExp.input + "");
    else
      window.alert("Verifique se sua geladeira está conectada na tomada e se o termostato está na temperatura certa.")
  }
  //buscar geladeira api
} 

function test(){


  //buscar os valores preenchidos passando os valores para o post na API
}